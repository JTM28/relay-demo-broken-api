"""Persistence-specific schemas intentionally left blank for future agent work.

API-facing request and response contracts live in the top-level ``contracts`` package
so later SQL and NoSQL implementations can introduce their own storage schemas without
changing the public HTTP surface first.
"""

__all__: list[str] = []
